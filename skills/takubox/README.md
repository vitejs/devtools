# takubox skill

An agent skill that teaches Claude Code how to build a full-featured
devtool with takubox.

## Opt-in

Until takubox publishes an installable skill, link this directory from
your Claude Code skills path. Example for the CLI:

```sh
ln -s "$PWD/skills/takubox" "$HOME/.claude/skills/takubox"
```
