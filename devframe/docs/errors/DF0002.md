---
outline: deep
---

# DF0002: Cannot Change Dock ID

## Message

> Cannot change the id of a dock. Use register() to add new docks.

## Cause

`DevToolsDockHost.update()` rejects id changes because docks are keyed by id.

## Fix

Keep the `id` on `update(patch)` identical to the registered value, or call `register()` with a new id (and optionally `force: true`).

## Source

- [`packages/devframe/src/node/host-docks.ts`](https://github.com/vitejs/devtools/blob/main/devframe/packages/devframe/src/node/host-docks.ts) — `DevToolsDockHost.update()` rejects id changes because docks are keyed by id; throws `DF0002` when the patch's `id` differs from the registered value.
- [`packages/devframe/src/node/diagnostics.ts`](https://github.com/vitejs/devtools/blob/main/devframe/packages/devframe/src/node/diagnostics.ts) — defines `DF0002`.
